
// An included file with app utility functions.
public class Utils : ScriptBase
{
    int notavar = 911;

    public bool Boing(int which = 0)
    {
        bool boinged = true;

        if (which == 0)
        {
            which = RandomInt(30, 60);
            boinged = true;
        }

        Print($"Boinged {which}");

        return boinged;
    }
}